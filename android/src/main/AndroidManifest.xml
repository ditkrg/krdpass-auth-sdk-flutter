<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The native core makes the PAR, token, JWKS, userinfo and revoke calls. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
